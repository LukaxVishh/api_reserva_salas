meta {
  name: 09_Criar sala campos faltando
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/salas
  body: json
  auth: none
}

body:json {
  {
    "nome": "Sem bloco"
  }
}

vars:pre-request {
  baseUrl: http://localhost:8080
}

tests {
  test("campos obrigatorios -> 400", function() {
    expect(res.getStatus()).to.equal(400);
  });
}
